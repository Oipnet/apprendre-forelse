# Environnement d'exécution : un petit projet PHP et le simulateur Docker (packages/simulateur-docker), empaqueté par environments/bin/build-env.sh
id: docker
title: Docker — simulateur (Dockerfile, Compose) et PHP 8.4
php: '8.4'
framework: docker
cache: []
