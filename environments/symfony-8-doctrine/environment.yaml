# Environnement d'exécution : le Symfony de base, plus Doctrine ORM et ses migrations.
# Il prolonge symfony-8 : ne vivent ici que les fichiers qu'il ajoute ou remplace.
id: symfony-8-doctrine
extends: symfony-8
title: Symfony 8.1 — Twig, Doctrine ORM (SQLite), PHPUnit
php: '8.4'
