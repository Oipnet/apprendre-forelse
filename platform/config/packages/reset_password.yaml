symfonycasts_reset_password:
    request_password_repository: App\Repository\ResetPasswordRequestRepository
    # Le lien reçu par email est valable une heure ; une nouvelle demande pour le même compte
    # n'est acceptée qu'après expiration de la précédente (pas de rafale d'emails).
    lifetime: 3600
    throttle_limit: 3600
