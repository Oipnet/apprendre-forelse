# Analyse statique du moteur, niveau 6, sans erreur connue ni baseline : la CI échoue sur toute erreur.
# Une erreur se corrige (code, ou PHPDoc inexact) ; elle ne se gèle pas.
includes:
    - vendor/phpstan/phpstan-symfony/extension.neon
    - vendor/phpstan/phpstan-doctrine/extension.neon

parameters:
    level: 6
    paths:
        - src
    tmpDir: var/cache/phpstan
    symfony:
        # Le conteneur compilé en dev (bin/console lint:container, ou n'importe quelle commande, le produit).
        containerXmlPath: var/cache/dev/App_KernelDevDebugContainer.xml
