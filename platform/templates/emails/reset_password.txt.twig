Bonjour {{ user.displayName }},

Pour choisir un nouveau mot de passe sur Forelse, ouvrez ce lien :

{{ url('app_reset_password', {token: resetToken.token}) }}

Il est valable {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }} et ne sert qu'une fois.

Si vous n'avez rien demandé, ignorez cet email : votre mot de passe reste inchangé.

Forelse · apprendre
